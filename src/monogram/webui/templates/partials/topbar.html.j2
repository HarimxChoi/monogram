<div class="topbar">
  <div class="topbar__brand">MONOGRAM</div>
  <div class="topbar__meta">
    <div><span>commit</span> <strong>{{ meta.commit_short }}</strong></div>
    <div><span>gen</span> <strong>{{ meta.generated_relative }}</strong></div>
    <div><span>vault</span> <strong>{{ meta.vault }}</strong></div>
  </div>
  <div class="topbar__actions">
    <div class="dot-encrypted">encrypted</div>
    <button class="btn-ghost" data-action="refresh">↻ Refresh</button>
    <button class="btn-ghost" data-action="auto-refresh">Auto · Off ▾</button>
  </div>
</div>
